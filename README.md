# Openclawd-pro-employee

Openclawd-pro-employee is the employee-facing area of the Openclawd Pro platform.

## Project status

This repository currently contains documentation only. Application scaffolding,
npm scripts, and database migrations have not been added yet, so commands such as
`npm install`, `npm run migrate`, or `npm start` are not available.

## Next steps (planned)

- Add the initial application scaffolding.
- Document the real setup steps once the implementation lands.
